version: '3.8'

services:
  grafana-toolkit:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - toolkit-data:/data
    environment:
      - DATABASE_PATH=/data/dashboards.db
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    restart: unless-stopped

volumes:
  toolkit-data:
